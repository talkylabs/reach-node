{
  "dependencies": {
    "jest": "^29.4.3",
    "localtunnel": "^2.0.2",
    "reach": "../../package"
  },
  "scripts": {
    "prepare": "tsc",
    "test": "jest lib --testPathIgnorePatterns=lib/webhook"
  }
}
